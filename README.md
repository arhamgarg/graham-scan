# Graham Scan with AVL Trees

This project implements a dynamic convex hull using an AVL tree to maintain points in Graham-scan order. It mirrors the structure of the referenced red-black-tree repository while swapping in an AVL-based balancing strategy.

## Build

```bash
make
```

## Run

```bash
./build/make/hull --self-test
./build/make/hull --benchmark
```
